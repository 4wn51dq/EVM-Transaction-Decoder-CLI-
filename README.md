# Ethereum Transaction Decoder CLI

- Mimicks etherscan, but this isnt an entire block explorer.
- Only a command line tool that decodes raw EVM chain transactions into 
a structured, human-readable way.

## Goals
- Decode raw hex transactions.
- Enforce Ethereum protocol invariants.
- Explain transaction intent.